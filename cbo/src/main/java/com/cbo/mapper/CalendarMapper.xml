<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.cbo.mapper.CalendarMapper">
  
  
 <insert id="insertWork" parameterType="com.cbo.calendar.model.CalendarDTO">
  insert into calendar (
    id, member_id, dept_id, title, content, start_time, end_time, create_date, allday
  ) values (
    sq_calendar_id.nextval,
    #{member_id},
    #{dept_id},
    #{title},
    #{content},
    #{start_time},
    #{end_time},
    sysdate,
    1
    
  )
</insert>

 <insert id="insertTime" parameterType="com.cbo.calendar.model.CalendarDTO">
  insert into calendar (
    id, member_id, dept_id, title, content, start_time, end_time, create_date, allday
  ) values (
    sq_calendar_id.nextval,
    #{member_id},
    #{dept_id},
    #{title},
    #{content},
    #{start_time},
    #{end_time},
    sysdate,
    0
    
  )
</insert>
<select id="findId" resultType="int">
	select id from member where user_id = #{saveid}
</select>

<select id="findDept" resultType="int">
	select dept_id from member where user_id = #{saveid}
</select>
  
  
  <resultMap id="calendarMap" type="com.cbo.calendar.model.CalendarDTO">
    <result property="id" column="id"/>
    <result property="title" column="title"/>
    <result property="content" column="content"/>
    <result property="start_time" column="start_time"/>
    <result property="end_time" column="end_time"/>
</resultMap>

  <select id="selectList" resultMap="calendarMap">
  select * from calendar where member_id = #{id}
  </select>
  
  <delete id="deleteWork" parameterType="com.cbo.calendar.model.CalendarDTO">
  delete from calendar where id = #{id}
  </delete>
  
<update id="updateWork" parameterType="com.cbo.calendar.model.CalendarDTO">
  update calendar set start_time = #{start_time}, end_time = #{end_time}+1, title = #{title}, content = #{content} where id = #{id}
  </update>
  
  <update id="saveWork" parameterType="com.cbo.calendar.model.CalendarDTO">
    update calendar
    set start_time = #{start_time},
        end_time = #{end_time}
    where id = #{id}
  </update>
  
  </mapper>




